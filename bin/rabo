#!/usr/bin/env php
<?php

declare(strict_types=1);

foreach ([__DIR__.'/../vendor/autoload.php', __DIR__.'/../../../autoload.php'] as $autoload) {
    if (is_file($autoload)) {
        require $autoload;
        break;
    }
}

if (! class_exists(Sifrious\Rabo\Cli\Application::class)) {
    fwrite(STDERR, "Run `composer install` first.\n");
    exit(2);
}

exit((new Sifrious\Rabo\Cli\Application())->run($argv));
